import { Component } from '@angular/core';
// import { HttpService } from '../../core/services/http';

@Component({
    moduleId: module.id,
    selector: 'as-home',
    templateUrl: 'home.html',
    styleUrls: [
        'home.css'
    ]
})
export class HomeComponent {
    // title = HttpService.showCode();
}
